#ifndef MAXSUB_ALG_HPP
#define MAXSUB_ALG_HPP

#include <vector>

class MaxSubArray {
public:
    int maxSubArraySumON(const std::vector<int>& nums);
    int maxSubArraySumON2(const std::vector<int>& nums);
    int maxSubArraySumON3(const std::vector<int>& nums);
};

#endif // MAXSUB_ALG_HPP
